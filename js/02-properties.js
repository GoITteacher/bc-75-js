/*
 * Властивості елемента (hero)
 * - Зображення
 * - Текст та textContent
 * https://picsum.photos/720?random=11
 */

//!======================================================
// const imageEl = document.querySelector('.hero__image');
// const random = Math.round(Math.random() * 50);
// imageEl.setAttribute('src', `https://picsum.photos/720?random=${random}`);
//!======================================================

/*
 * Атрибути
 * - get(ім'я-атрибута)
 * - set(ім'я-атрибута)
 * - remove(ім'я-атрибута)
 * - has(ім'я-атрибута)
 */

// const heroTitleEl = document.querySelector('.hero__title');

// heroTitleEl.setAttribute('alt', 'Hello world');
// heroTitleEl.setAttribute('data-id', 'Hello world');
// heroTitleEl.setAttribute('test', 'Hello world');

// heroTitleEl.removeAttribute('data-id');

//!======================================================
/*
 * Data-атрибути
 */
// const actions = document.querySelectorAll('.actions button');

// const btnElem = actions[0];

// btnElem.dataset.hello = 'hello world';
// btnElem.dataset.test = 'hello world';

// console.log(btnElem.dataset);
// console.log(btnElem.dataset.action);
// console.log(btnElem.dataset.title);
// console.log(btnElem.dataset.userId);
